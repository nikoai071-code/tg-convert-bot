# "..." обязателен: иначе список пакетов провайдера затирается — Python/ffmpeg могут пропасть из образа.
[phases.setup]
nixPkgs = ["...", "ffmpeg"]
aptPkgs = ["ffmpeg"]

[variables]
NIXPACKS_PYTHON_VERSION = "3.12"

[start]
cmd = "python main.py"
