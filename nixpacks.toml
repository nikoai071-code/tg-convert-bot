# "..." сохраняет пакеты провайдера (Python и др.) — без него они затираются.
[phases.setup]
nixPkgs = ["...", "ffmpeg", "ffmpeg.lib"]

[variables]
NIXPACKS_PYTHON_VERSION = "3.12"

[start]
cmd = "python main.py"
